function KMeans(canvas, meansCount) {

    function process() {
        /* This should return an array of mean colors */
        return [{
            color: Color(255, 0, 0)
        }];
    }

    return {
        process: process
    }
}